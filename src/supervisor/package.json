{
  "name": "agent-sdk-acp-supervisor",
  "version": "0.1.0",
  "private": true,
  "description": "Thin stdio↔HTTP(POST+SSE) bridge for ACP agents (claude-agent-acp, codex-acp)",
  "type": "commonjs",
  "main": "supervisor.js",
  "bin": {
    "acp-supervisor": "./supervisor.js"
  },
  "scripts": {
    "start": "node supervisor.js"
  },
  "dependencies": {
    "@agentclientprotocol/claude-agent-acp": "0.64.2",
    "@agentclientprotocol/codex-acp": "1.1.9",
    "opencode-ai": "^1.4.3"
  },
  "//_disabledDependencies": "The entries below are not currently used. They live in an npm-ignored key because JSON has no comments. Move an entry into `dependencies` to enable its agent type.",
  "_disabledDependencies": {
    "@google/gemini-cli": "^0.37.2",
    "cline-acp": "^0.1.6",
    "deepagents-acp": "^0.1.8"
  }
}
