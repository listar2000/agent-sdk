{
  "name": "agent-sdk-acp-supervisor",
  "version": "0.1.0",
  "private": true,
  "description": "Thin stdio↔HTTP(POST+SSE) bridge for ACP agents (claude-agent-acp, codex-acp)",
  "type": "commonjs",
  "main": "supervisor.js",
  "bin": {
    "acp-supervisor": "./supervisor.js"
  },
  "scripts": {
    "start": "node supervisor.js"
  },
  "dependencies": {
    "@agentclientprotocol/claude-agent-acp": "^0.27.0",
    "@agentclientprotocol/codex-acp": "^1.0.2",
    "opencode-ai": "^1.4.3"
  },
  "//_disabledDependencies": "claude + codex + opencode ACP runtimes are used. The entries below are commented out (JSON has no comments, so they live in an npm-ignored key) to keep the image small. Move any back into `dependencies` to re-enable.",
  "_disabledDependencies": {
    "@google/gemini-cli": "^0.37.2",
    "cline-acp": "^0.1.6",
    "deepagents-acp": "^0.1.8"
  }
}
